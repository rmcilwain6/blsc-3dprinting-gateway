# Branding Assets

Place BLSC logo files here. Expected files:

- `BLSC_Logo_Dark.png` — for use on dark backgrounds (the primary use case)
- `BLSC_Logo_Light.png` — for use on light backgrounds
- Any other brand assets

These are referenced in the UI but not committed to the repo.
